# Reference material only - not part of the eLamX web migration itself,
# and each is its own separate git checkout (has its own .git/).
/eLamX2/
/View3DSuite/
/eLamX30_Hauptfenster.png

# Binary eLamX 3.x distribution (~180 MB incl. a bundled JRE), used to generate
# the golden-master reference data - see elamx-core/core/tests/golden/README.md.
/elamx/

# Claude Code local project config (personal, not project source).
/.claude/

# The desktop shell's own build products: Electron itself (~250 MB of
# node_modules) and the installers it produces (~110 MB each). Both are
# reproducible from desktop/package.json - see the README.
/desktop/node_modules/
/desktop/release/
